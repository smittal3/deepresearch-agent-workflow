# Deep Research Agent — dependencies
# Python 3.10+ required, 3.11 recommended.
# Single provider: OpenRouter powers ALL LLM calls + the image-PDF vision OCR (one key).
# Versions pinned to a verified-working set (tested on Python 3.11).

streamlit==1.58.0
langchain==1.3.9
langchain-openai==1.3.2
langgraph==1.2.5
PyMuPDF==1.27.2.3
pypdf==6.13.2
python-docx==1.2.0
python-pptx==1.0.2
tavily-python==0.7.26
arxiv==4.0.0
python-dotenv==1.2.2
